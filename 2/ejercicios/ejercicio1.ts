//  Ejercicio 1: Tipos Básicos
// Crea un archivo llamado ejercicio1.ts y realiza lo siguiente:

// Declara variables con tipos explícitos:

// nombre: string con tu nombre
// edad: number con tu edad
// esEstudiante: boolean (true o false)
// nota: number con un valor decimal (ej: 8.5)
let nombre: string = "javier"
let edad: number = 33
let esEstudiante: boolean = true
let nota: number = 8

// Crea un array de números llamado calificaciones con al menos 3 notas
let calificaciones: number[] = [8, 8.5, 9]

// Crea un array de strings llamado materias con nombres de materias
let materias: string[] = ["desarrollo orientado a objetos", "analisis matematico", "desarrollo web"]

// Usa console.log() para mostrar todas las variables
console.log(nombre, edad, esEstudiante, nota, calificaciones, materias)

